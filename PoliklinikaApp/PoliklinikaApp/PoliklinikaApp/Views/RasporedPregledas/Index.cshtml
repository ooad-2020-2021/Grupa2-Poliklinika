@model IEnumerable<PoliklinikaApp.Models.RasporedPregleda>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.zakazanTermin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.slobodanTermin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.otkazanTermin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vrijeme)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.zakazanTermin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.slobodanTermin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.otkazanTermin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.vrijeme)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_rasporeda">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_rasporeda">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_rasporeda">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
